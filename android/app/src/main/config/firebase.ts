// src/config/firebase.ts
import auth from '@react-native-firebase/auth';

// Firebase config - lấy từ Firebase Console
export const firebaseConfig = {
  // Config sẽ tự động đọc từ google-services.json
};

export { auth };